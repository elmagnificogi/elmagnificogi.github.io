{
  "name": "elmagnificogi-blog",
  "private": true,
  "scripts": {
    "build": "jekyll build",
    "pagefind": "pagefind",
    "pagefind:ui": "pagefind --site pagefind-stub --output-path pagefind --quiet",
    "build:search": "jekyll build && pagefind"
  },
  "devDependencies": {
    "pagefind": "^1.5.2"
  }
}
